# DÉPANN’TECH — version administration V6

Administration: `gestion.html`
Ancienne adresse `admin.html` redirige vers `gestion.html`.

Remplacer `index.html`, `admin.html`, `gestion.html` et conserver `data.json` et `uploads/`.
